# Servidor MCP de DT-OS
PORT=3470
# Base de la API de bot de DT-OS (mismo VPS)
DTOS_API_BASE=http://localhost:3004/api/webhook
# API key del bot (igual que en backend/.env)
BOT_API_KEY=dt-bot-secret-key-2024
# Token secreto del MCP: va en la URL del conector (/mcp/<MCP_TOKEN>). Genera uno largo.
MCP_TOKEN=cámbiame-por-un-token-largo-aleatorio
